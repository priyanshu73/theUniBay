Terminal command to create virtual Environment.
-> python -m venv venv

Activate virtual Environment   (before running)
-> venv/scripts/activate    

install dependencies: pip install -r requirements.txt

Inside the venv
-> .\install_depts.bat
